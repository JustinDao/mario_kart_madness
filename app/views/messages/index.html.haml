%h1 Bulletin
.bulletin
  %table.messages

  - if current_user
    .text
      = text_field_tag :text
    .letter-count 140
    .submit
      = button_tag "Post", :class => "message_submit"
  - else
    .login-post
      = link_to "Login to Post!", login_path