%h1 Listing consoles
%table
  %thead
    %tr
      %th Name
  %tbody
    - @consoles.each do |console|
      %tr
        %td= link_to console[get_field_index(Console, "gcname")], console_path(console[0])
%br
  = link_to "Back", root_path