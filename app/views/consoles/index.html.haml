%h1 Listing consoles
%table
  %thead
    %tr
      %th Name
      %th
      %th
  %tbody
    - @consoles.each do |console|
      %tr
        %td= console[get_field_index(Console, "gcname")]
        %td= link_to 'Show', console_path(console[0])
        %td= link_to 'Edit', edit_console_path(console[0])
%br
  = link_to 'New Console', new_console_path
  |
  = link_to "Back", root_path